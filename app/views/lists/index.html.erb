<div class="container">
  <h1>My movie lists</h1>

  <% @lists.each do |list| %>
    <h2><%= link_to list.name, list_path(list) %></h2>
  <% end %>

  <%= link_to "Add New List", new_list_path %>
</div>
